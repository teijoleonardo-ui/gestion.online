/**
 * El scroll vive en <main> (dashboard); esta página usa cabecera sticky como el resto.
 */
export default function RetencionesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
